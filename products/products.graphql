type Query{
    products:[Product]
    productsByPrice(max:Float!,min:Float!):[Product]
    product(id:ID!):Product
}

type Product {
  id: ID!
  description: String!
  reviews: [Review]
  price: Float!
}

type Review {
  rating: Int!
  comment: String
}
